const nextConfig = {
    reactStrictMode: true,
    // experimental: {
    //     serverActions: true,
    // },
    // Disable remote Google font fetching
    optimizeFonts: false,
};

module.exports = nextConfig;
